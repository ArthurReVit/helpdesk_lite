List Requester Tickets
---
tags:
  - Tickets
security:
  - Bearer: []
parameters:
  - name: requester_id
    in: path
    type: string
    required: true
    description: UUID of the requester
responses:
  200:
    description: A list of tickets created by the user
    schema:
      properties:
        tickets:
          type: array
          items:
            properties:
              id:
                type: integer
              requester_id:
                type: string
              assignee_id:
                type: string
              title:
                type: string
              description:
                type: string
              status:
                type: string
              priority:
                type: string
              created_at:
                type: string
                format: date-time
              updated_at:
                type: string
                format: date-time
  401:
    description: Unauthorized
  403:
    description: Forbidden - You can only view your own tickets
  404:
    description: User not found
