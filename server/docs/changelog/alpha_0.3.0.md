# Alpha 0.3.0

## Changes

- Implemented and documented the `GET /api/auth/me` and `POST /api/auth/logout` endpoints.
- Documented the authentication endpoints in the [Auth API Documentation](../api/auth.md).
- Created Swagger specifications for the new authentication features under `docs/swagger/auth/`.
- Integrated the Swagger documentation with the API using `swag_from` for cleaner route management.

## Bug fixes

- No bugs identified at the moment.

## Notes

- This update concludes the initial authentication module documentation.
- All auth-related Swagger definitions are now modularized.
